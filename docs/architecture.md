# Architecture — current state

> Describes how the system is built **right now**. Update this in the *same*
> change that alters the architecture. Describe shape (modules, flow), not
> line-level detail — the code is the source of truth for details.

**Status: decided, not yet built.**

The intended shape is ratified in `docs/decisions.md` (ADR-001 … ADR-007), but
**no code exists yet**. This document describes what is built, so it stays a stub
until the first slice lands — it must never document intentions. Decisions live
in the ADRs; the order of work lives in `docs/plan.md` under *Roadmap*.

It fills in as code arrives — module map, call flow, conventions, testing
conventions, the "where to make a change" map — describing only what actually
exists.

**Last verified against commit:** _none yet_
